All Go compilation errors have been fixed. Here's a summary of the changes:

**1. compliance-service** (`services/compliance-service/`):
- Changed `compliance.go` package from `package compliance` to `package main`
- Removed self-import from `main.go`
- Renamed `Config` to `ServerConfig` in `main.go` to avoid collision with `Config` in `compliance.go`
- Removed unused variables (`ctx`, `repo`)
- Removed incorrect `event.DataTypes` reference
- Added missing `net/http` import to `compliance.go`

**2. policy-service** (`services/policy-service/`):
- Changed `policy_engine.go` package from `package policy` to `package main`
- Removed self-import from `main.go`
- Renamed `Config` to `ServerConfig` in `main.go` to avoid collision
- Fixed malformed function signature: `func (s, substr string) bool` → `func hasPrefixOrSuffix(s, substr string) bool`
- Fixed `contains` function call to use `hasPrefixOrSuffix`
- Removed unused variables (`ctx`, `repo`)

**3. api-gateway** (`services/api-gateway/`):
- Fixed import path alias: `handlers` → `gatewayhandlers` and updated all references

**4. auth-service** (`services/auth-service/handler/oidc.go`):
- Fixed unused variables (`oidcConfig`, `ctx`)

**5. m365-integration-service** (`services/m365-integration-service/main.go`):
- Fixed unused variable (`ctx`)

**6. registry-service** (`services/registry-service/handler/agent.go`):
- Changed `FindByAgentID` → `FindByAgent` (correct method name)
- Fixed `agent.OrganizationID != nil` → `agent.OrganizationID != ""` (it's a string, not a pointer)
- Removed unnecessary dereference `*agent.OrganizationID`
