Done. I've made the single change requested:

**Created `retry_with_backoff()` function:**
- Location: Lines 224-245 (after `safe_tr()`, before `ERROR_MEMORY`)
- Signature: `retry_with_backoff(command, max_retries, base_delay)`
- Implements exponential backoff: `sleep $((base_delay * 2**(attempt-1)))`
- Returns 0 on success, 1 on final failure

**Replaced raw retry loop in `claude_do()`:**
- The old `while [ $attempt -lt 3 ]; do ... sleep 5; done` loop now uses `retry_with_backoff "_claude_retry_cmd" 3 5`
- Delays: 5s → 10s → 20s (exponential)

**Verification:**
```bash
declare -f retry_with_backoff >/dev/null && echo 'function exists'
# Output: function exists
```

Syntax: `PASS`
