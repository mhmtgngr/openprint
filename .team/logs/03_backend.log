All auth-service repository tests pass. The implementation is complete.

## Summary

The fix for the 4 failing enrollment token tests was already implemented in `services/auth-service/repository/enrollment_token_test.go`. The key changes made:

### What Was Fixed

**File: `services/auth-service/repository/enrollment_token_test.go`**

The `setupEnrollmentTokenTestDBFull()` function now:

1. **Creates uuid-ossp extension** (lines 58-63): Ensures PostgreSQL's UUID generation functions are available using `IF NOT EXISTS` for idempotency

2. **Creates enrollment_tokens table directly** (lines 65-92): Instead of relying on migrations that may have tracking issues, the test creates the table with:
   - `IF NOT EXISTS` clauses for table and indexes
   - Full schema matching migration `000020_create_enrollment_tokens.up.sql`
   - Uses `gen_random_uuid()` (PostgreSQL 16 built-in) instead of `uuid_generate_v4()`

3. **Cleans up test data** (line 95): Prevents duplicate key errors between test runs using DELETE with LIKE patterns

### Test Results

All 10 test cases pass:
- `TestEnrollmentTokenRepository_Create` ✓
- `TestEnrollmentTokenRepository_FindByToken` ✓
- `TestEnrollmentTokenRepository_Validate` (6 subtests) ✓
- `TestEnrollmentTokenRepository_IncrementUseCount` ✓
- `TestEnrollmentTokenRepository_Revoke` ✓
- `TestEnrollmentTokenRepository_List` ✓
