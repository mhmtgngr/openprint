Fixed. The `TestManager_ACSHandler/wrong_method` test now passes. 

The issue was that `ACSHandler` didn't check the HTTP method before processing the request. I added a method check that returns `http.StatusMethodNotAllowed` (405) for non-POST requests.
