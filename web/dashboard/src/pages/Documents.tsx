/**
 * Documents page - wires the full Documents feature from features/documents
 */
import { Documents as DocumentsFeature } from '@/features/documents';

export const Documents = () => {
  return <DocumentsFeature />;
};
